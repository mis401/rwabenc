// import { createReducer, on } from "@ngrx/store";
// import { initialDoctorState } from "./doctor.state";
// import { loginDoctorFailure, loginDoctorSuccess } from "./doctor.actions";

// export const doctorReducer = createReducer(
//     initialDoctorState,
//     on(loginDoctorSuccess, (state, {doctor, token}) => ({
//         ...state, 
//         doctor: doctor,
//         token: token
//     })),
//     on(loginDoctorFailure, (state, {error}) => ({
//         ...state,
//         error: error
//     }))
    
// );
